import Foundation

extension ModelData {
    enum Constants {
        static let modelData = ModelData()
        static let landmarks = ModelData().landmarks
        static let features = ModelData().features
    }
}
